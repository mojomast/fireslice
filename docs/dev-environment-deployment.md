# Historical Note: old ussycode deployment write-up

This file used to document an older `ussycode` deployment attempt involving Cloudflare tunnels, SSH-first flows, and routussy.

It is not current `fireslice` deployment guidance.

For current deployment truth, use:

- `README.md`
- `docs/fireslice.md`
- `docs/self-hosting.md`
- `deploy/docker/fireslice-control/README.md`

If the old historical notes are still needed, recover them from Git history rather than treating them as current operating instructions.
